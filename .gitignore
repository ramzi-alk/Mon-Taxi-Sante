node_modules
.output
.vinxi
.tanstack
.vercel/output
dist

# Vinxi/Vite generated config cache files
app.config.timestamp_*.js
app.config.timestamp_*.mjs

# Environment variables
.env
.env.local
.env.*.local

# Build artifacts
*.tsbuildinfo

# Ce projet utilise pnpm (pnpm-lock.yaml) — ne pas laisser un `npm install`
# local recréer un package-lock.json qui traînerait dans le repo.
package-lock.json
yarn.lock

# Playwright
test-results/
playwright-report/
blob-report/
playwright/.cache/

# OS
.DS_Store
Thumbs.db
node_modules
